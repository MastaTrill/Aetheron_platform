# Historical Status Snapshot — July 16, 2026

> **Superseded status document.** This file preserves the July 16 presale-recovery state for historical context. It is not the current deployment source of truth. See `PROJECT_STATUS.md`, `docs/AETHERON_CONTRACT_REGISTRY.json`, `smart-contract/deployments/aeth-base.json`, `smart-contract/deployments/presale-base.json`, and `presale-config.js` for current status. A corrected Base presale was subsequently deployed and source-verified on July 18, 2026; current public launch authorization remains disabled.

**Snapshot date:** July 16, 2026

## Deployment state at this snapshot

| Area | Status | Details |
|---|---:|---|
| AETH token | ✅ Deployed | Base mainnet: `0xecf7E17faE148C01E1b5008A31Dfd2d1B6608E4e` |
| Recorded presale | 🔴 Invalid | `0xA7aa360d2F00Cf4130B3244D0A13AE32a49ab07C` returns token `0xAb5ae0D8f569d7c2B27574319b864a5bA6F9671e`, not the configured Base AETH token |
| Public purchase flow | ⛔ Disabled | Both frontend configuration copies had no active presale address and recorded the invalid address separately |
| Invalid presale ETH raised | ✅ Zero observed | Disposable Base fork read `weiRaised = 0`; no real ETH was spent during validation |
| New AETH at invalid presale | ⚠️ 50,000,000 AETH observed | These tokens were sent to a contract whose immutable `token()` points elsewhere; recovery had to be established before any further token movement |
| Invalid presale state | ⚠️ Open but unused | `cancelled = false`, `finalized = false`; guarded owner-only cancellation tooling was prepared |
| Replacement presale | ⏳ Required at this snapshot | A corrected replacement was later deployed at `0xe0A3B6368312dFd3E7E76202e673f895f8235A3d` |
| Frontend purchase logic | ✅ Fixed in PR #150 | Real hard cap, cumulative wallet limit, live inventory, static simulation, and gas estimation |
| OpenZeppelin remediation | ✅ Fixed in PR #150 | Both packages resolve to `4.9.6`; lockfile regenerated by CI |
| Install, compile, and tests | ✅ Passing in CI | `npm ci`, Solidity compilation, unit tests, frontend regression tests, and disposable Base fork safety test passed |
| Liquidity / public launch | ⛔ Blocked | This remained blocked in this snapshot and current release authorization is still separately gated |

## Confirmed root cause

Multiple Base deployment/funding scripts reused the hard-coded presale address `0xA7aa…b07C` after deploying a new AETH token. They excluded and funded that address without first checking `presale.token()`. The presale's immutable token reference points to `0xAb5a…9671e`, while the new Base AETH token is `0xecf7…8E4e`.

The unsafe scripts were disabled in PR #150. The replacement script deploys a fresh presale and validates chain ID, token ownership, constructor linkage, treasury linkage, tax exclusion, and inventory before it writes frontend configuration.

## Safety actions completed by this snapshot

1. Disabled the public presale configuration on `main`.
2. Marked the then-recorded invalid presale non-launchable.
3. Recorded the invalid contract, linked token, 50,000,000 expected AETH balance, zero raised ETH, and cancellation/finalization state.
4. Added a guarded owner-only cancellation script for the invalid presale.
5. Added read-only Base verification and disposable fork testing.
6. Closed stale, non-mergeable Snyk PRs and recreated the OpenZeppelin fix from current `main`.
7. Disabled discovered hard-coded token/presale deployment and funding scripts.

## Sequence that remained at this snapshot

1. Complete legacy-ABI inspection and confirm the owner plus every available recovery method on the invalid contract.
2. After owner review, cancel the invalid presale using the guarded command; do not expose the private key in chat or GitHub.
3. Establish a safe recovery path for the 50,000,000 new AETH held at the invalid address.
4. Run the corrected replacement deployment in `DRY_RUN=true` mode with explicitly selected sale parameters.
5. Deploy a fresh presale linked to `0xecf7…8E4e`, exclude it from tax, and fund it atomically.
6. Re-run bytecode/state verification and the Base fork purchase simulation.
7. Verify source and constructor arguments on BaseScan.
8. Perform one deliberately small mainnet purchase only after all checks pass and the wallet owner explicitly approves the transaction.

Steps 4–8 later produced the recorded corrected presale and owner-smoke evidence. Those historical actions do **not** replace the current release gates in #217 and #219.

## Safety note

Do not send ETH or additional AETH to `0xA7aa360d2F00Cf4130B3244D0A13AE32a49ab07C`. Never commit private keys, RPC secrets, or wallet seed phrases.
