console.error("This legacy Polygon mainnet presale deployer is disabled.");
console.error("It uses obsolete token, network, constructor, and sale values and must not be used.");
console.error("Use the protected GitHub Actions workflow: Deploy Corrected Base Presale.");
console.error("Run a dry run first; live Base deployment still requires explicit owner authorization.");
process.exit(1);
