console.error("This script is disabled because it paired a newly deployed Base token with a hard-coded presale that was constructed for a different token.");
console.error("Use scripts/deploy-base-presale-safe.mjs after compiling and completing its dry run.");
process.exit(1);
