console.error("This legacy deployment script is disabled because it defaults to the wrong token/network and does not match the current AetheronPresaleV2 constructor.");
console.error("Run npm run deploy:base-presale:dry-run, then npm run deploy:presale only after reviewing all parameters.");
process.exit(1);
