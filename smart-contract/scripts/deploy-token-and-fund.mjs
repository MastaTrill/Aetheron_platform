console.error("This script is disabled because it transferred AETH to a hard-coded presale before checking the presale's immutable token linkage.");
console.error("Use scripts/deploy-base-presale-safe.mjs for the corrected Base deployment flow.");
process.exit(1);
