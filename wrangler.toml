# This Wrangler config is for the Cloudflare Worker service. The connected
# Cloudflare Pages project uses its dashboard build settings and ignores this
# file because it intentionally omits pages_build_output_dir.
name = "aetheron"
compatibility_date = "2026-03-30"

[assets]
directory = "dist"
